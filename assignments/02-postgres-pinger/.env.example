# COPY AND CHANGE IT (cp .env.example .env && vim .env)
POSTGRES_PASSWORD=postgres-demo-password
DB_USER=pinger_user
DB_PASSWORD=pinger-demo-password
POLL_INTERVAL_SECONDS=300
LOG_FILE=/logs/pinger.log
